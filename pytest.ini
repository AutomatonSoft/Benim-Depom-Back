[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = tests.py test_*.py *_tests.py
addopts = -ra
markers =
    unit: isolated tests without database or external I/O
    integration: API and service tests using pytest's temporary database
    e2e: complete user journeys through real Django routes and a temporary database
